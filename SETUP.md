Quick start for GameTask frontend

1. Install deps: `npm install`
2. Copy `.env.example` to `.env` and set `VITE_API_URL` / `VITE_API_WS` if needed
3. Run dev server: `npm run dev`

The app uses `socket.io-client` to connect to the backend for realtime gameplay.
